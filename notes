use: 
Use Git & GitHub
Repository Pattern (For Laravel)
Advanced Routing (For Laravel)
Service Providers (For Laravel)

on login
check if user isBlocked

user
    add post
        check if user has reached max number of posts
        does it mean nobody can post if maxPostCount has met

required to show images in img tag
    php artisan storage:link

make migration
    php artisan make:migration create_comments_table
show model
    php artisan model:show User
